services:
  vibration:
    build: .
    container_name: vibration_app
    ports:
      - "8001:8001"
    volumes:
      - ./templates:/app/templates
    environment:
      - SESSION_SECRET=${SESSION_SECRET:-vibration-platform-secret-key-change-in-production}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "https://vap.gcmeapps.com:8001/login-page"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
